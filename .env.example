# Non-secret configuration only — never put tokens or passwords here.
# Values are readable from a decompiled APK.
API_BASE_URL=http://10.0.2.2:3000/api/v1
SENTRY_DSN=
ENV=development

# Optional override for OpenAPI generation (defaults to local stub):
# OPENAPI_SPEC_URL=https://api.example.com/openapi.json
